<% content = form.object %>

<div class="row column">
  <%=
  form.translated(
      :text_area,
      :body,
      tabs_id: id,
      label: content.label,
      rows: 4
  )
  %>
</div>

<%= form.hidden_field :id %>
<%= form.hidden_field :section_id %>
